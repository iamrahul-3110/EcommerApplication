spring.application.name=ecommerceapp
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:ecommercedb

#  JPA Hibernate Configuration it will show sql statements in the console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# create-drop, create, update, validate, none these are the options for ddl-auto- and create-drop is default one: this is used in debugging phase not in production
# spring.jpa.hibernate.ddl-auto=create-drop


